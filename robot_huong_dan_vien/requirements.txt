# --- CÁC THƯ VIỆN CỐT LÕI (CORE DEPENDENCIES) ---
google-genai>=2.0.0
gTTS>=2.5.0
pygame>=2.6.0
python-dotenv>=1.0.0

# --- NÂNG CẤP V2.0 (ADVANCED UPGRADES) ---
edge-tts>=6.1.0            # Giọng đọc AI Studio Neural tiếng Việt (Hoài My / Nam Minh)
SpeechRecognition>=3.10.0   # Nhận diện giọng nói tiếng Việt qua Micro (Voice-to-Voice)
sounddevice>=0.5.0         # Thu âm phần cứng Micro đa nền tảng thời gian thực (Python 3.10-3.14)
numpy>=1.24.0              # Xử lý ma trận dữ liệu âm thanh số
rapidfuzz>=3.0.0           # Thuật toán so khớp mờ Fuzzy Matching cho Smart Cache

# --- TÍCH HỢP PHẦN CỨNG (HARDWARE INTEGRATION) ---
pyserial>=3.5              # Giao tiếp Serial (USB) với bo mạch điều khiển động cơ Matrix Mini R4

